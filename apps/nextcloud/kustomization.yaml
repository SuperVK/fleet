apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nextcloud
resources:
  - namespace.yaml
  - helmrelease.yaml
  # Created from secrets.sops.yaml.example BEFORE first push (README §Bootstrap).
  # Do not commit the unencrypted template as secrets.sops.yaml.
  - secrets.sops.yaml
  - import-job.yaml
